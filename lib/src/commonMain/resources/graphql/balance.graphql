query GetAddressBalance($address: String!) {
    address(address: $address) {
        address
        balance {
            coinType {
                repr
            }
            coinObjectCount
            totalBalance
        }
    }
}
