query GetAddressBalances($address: String!) {
    address(address: $address) {
        balances {
            nodes {
                coinType {
                    repr
                }
                coinObjectCount
                totalBalance
            }
        }
    }
}
